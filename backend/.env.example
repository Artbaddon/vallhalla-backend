# MySQL Database Configuration (existing)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=""
DB_NAME=vallhalla_db
DB_PORT=3306

# MongoDB Configuration (new for packages)
MONGODB_URI=mongodb://localhost:27017/vallhalla_packages

# JWT Configuration
JWT_SECRET=your_jwt_secret_key

# Server Configuration
PORT=3000
NODE_ENV=development
